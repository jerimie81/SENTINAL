[project]
name = "sentinal"
version = "0.2.0"
description = "SENTINAL portable offline AI core"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]

addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]
